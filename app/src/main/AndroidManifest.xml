<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.medicus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".PatientWeekSchedule"
            android:theme="@style/AppTheme.Dark" />
        <activity android:name=".PatientTodaySchedule"
            android:theme="@style/AppTheme.Dark"  />
        <activity
            android:name=".PatientDashboardActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".DoctorDashboardActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".DoctorAddPatientActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".DoctorAddPrescriptionActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".DoctorSignupActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".DoctorLoginActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".SignupActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>